<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 ">
            <form action="/create" method="post">
                <input class=" block w-full px-5 py-3 rounded-lg text-white bg-zinc-800 outline-none " type="text"
                    name="title" id="title" placeholder="Your Title Here...">
                <textarea
                    class="block w-full px-5 py-3  mt-2 text-white rounded-lg bg-zinc-800  outline-none resize-none"
                    name="detail" id="" placeholder="Your Text Here...."></textarea>
                <input class="mt-5 bg-blue-800 rounded-lg px-4 py-4" type="submit" value="Create Task">
            </form>

        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap ">
            <% if(files.length>0){%>
                <% files.forEach(function(val){%>
                    <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800 ">
                        <h1 class=" text-white text-3xtracking-tigj=hter"><%= val%></h1>
                        <p class="text-blue-500 mt-2">read more</p>
                    </div>

                <%})%>

            <%} else{%>
                <h1 class="text-zinc-500">no task here</h1>

            <%}%>

        </div>
    </div>

</body>

</html>